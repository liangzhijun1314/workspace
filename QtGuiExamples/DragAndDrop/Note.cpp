

/*
app 说明：该程序的功能
    1、外部文件拖拽并显示文本内容到TextEdit中，主要想展示的是如何实现外部文件的拖拽方式 dragEnterEvent dropEvent
    2、记录并显示上次窗口关闭时的geometry或者几何形状  readSettings(); writeSettings();
    3、重定向输出信息到文件中  qInstallMsgHandler(customMessageHandler);
    4、Qt中实现启动画面，当一个程序的启动比较耗时的时候，为了不让用户枯燥的等待或者是误以为程序运行异常了，所以我们都会在启动比较耗时的程序中加上启动界面 QSplashScreen
    5、自定义滑动的开关（网上内容）  SwitchControl
    6、自定义信号槽
*/
